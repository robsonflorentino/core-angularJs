define([
    // Requerindo bibliotecas necessárias
    'angular',
    'loDash',
    'app-modules',
    'app-routes'
], function(angular) {
	//console.log("require.s.contexts._.defined", require.s.contexts._.defined);
    angular.bootstrap(document, ['app']);
});